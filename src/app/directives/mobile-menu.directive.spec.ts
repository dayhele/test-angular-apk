import { MobileMenuDirective } from './mobile-menu.directive';

describe('MobileMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new MobileMenuDirective();
    expect(directive).toBeTruthy();
  });
});
