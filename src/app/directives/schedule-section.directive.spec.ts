import { ScheduleSectionDirective } from './schedule-section.directive';

describe('ScheduleSectionDirective', () => {
  it('should create an instance', () => {
    const directive = new ScheduleSectionDirective();
    expect(directive).toBeTruthy();
  });
});
